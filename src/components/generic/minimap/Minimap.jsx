import React from 'react';
import Tooltip from '../tooltip/Tooltip';

import './Minimap.scss';

const Minimap = () => (
  <Tooltip fieldName="generic.tooltips.productnetwork.minimap">
    <div className="minimap">
      <div className="inner" />
    </div>
  </Tooltip>
);

export default Minimap;
